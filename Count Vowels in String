#include <stdio.h>
int main() {
    char s[100];
    int count = 0;
    scanf("%s", s);
    for(int i=0; s[i]; i++)
        if(strchr("aeiouAEIOU", s[i])) count++;
    printf("Vowels = %d", count);
    return 0;
}
