#include <stdio.h>
int main() {
    int n, temp, rev = 0;
    scanf("%d", &n);
    temp = n;
    while(temp) {
        rev = rev * 10 + temp % 10;
        temp /= 10;
    }
    printf(n == rev ? "Palindrome" : "Not Palindrome");
    return 0;
}
